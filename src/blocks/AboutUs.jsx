import React from "react";

import "./Infini.scss";

const AboutUs = ({ data, locale }) => {

  return (
    <section className="aboutus">
      <section className="landing" id="">zilliz logo</section>
      <section className="mission" id="mission">mission</section>
      <section className="history" id="history">history</section>
      <section className="friends" id="friends">friends</section>
    </section>
  );
};

export default AboutUs;
